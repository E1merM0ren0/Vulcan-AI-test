// vulcan_gpt: placeholder - engine reimplementation in progress.
// Ports the original gpt2-style byte LM onto the rebuilt engine.
#include <iostream>
int main() {
    std::cout << "vulcan_gpt: not yet reimplemented\n";
    return 0;
}
